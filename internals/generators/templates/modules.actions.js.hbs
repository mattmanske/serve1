//-----------  Imports  -----------//

import { action, createActionConstants } from 'modules/helpers'

//-----------  Definitions  -----------//

export const {{ constantCase name }} = createActionConstants('{{ constantCase name }}')

//-----------  {{ titleCase name }} Actions  -----------//

export const {{ lowerCase name }}Actions = {
}

//-----------  Saga Actions  -----------//

export const sagaActions = {
  success: (data) => {
    return action({{ constantCase name }}.SUCCESS, { data })
  },
  failure: (error = null) => {
    return action({{ constantCase name }}.FAILURE, { error })
  }
}
