//-----------  Store  -----------//

const selectionsReduxer = (state = {}, action) => {
  switch (action.type){
  case 'SOMETHING':
    return state
  default:
    return state
  }
}

//-----------  Export  -----------//

export default selectionsReduxer
