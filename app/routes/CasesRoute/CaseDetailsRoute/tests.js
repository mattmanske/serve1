//-----------  Imports  -----------//

import React       from 'react'
import { shallow } from 'enzyme'

import CaseDetailsRoute from '../index'

//-----------  Tests  -----------//

describe('<CaseDetailsRoute />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
