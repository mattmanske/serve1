//-----------  Imports  -----------//

import React       from 'react'
import { shallow } from 'enzyme'

import JobDetailsRoute from '../index'

//-----------  Tests  -----------//

describe('<JobDetailsRoute />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
