//-----------  Imports  -----------//

import React       from 'react'
import { shallow } from 'enzyme'

import AttemptDetailsRoute from '../index'

//-----------  Tests  -----------//

describe('<AttemptDetailsRoute />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
