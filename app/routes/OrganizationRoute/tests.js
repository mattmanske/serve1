//-----------  Imports  -----------//

import React       from 'react'
import { shallow } from 'enzyme'

import OrganizationRoute from '../index'

//-----------  Tests  -----------//

describe('<OrganizationRoute />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
