//-----------  Imports  -----------//

import React       from 'react'
import { shallow } from 'enzyme'

import ClientDetailsRoute from '../index'

//-----------  Tests  -----------//

describe('<ClientDetailsRoute />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
