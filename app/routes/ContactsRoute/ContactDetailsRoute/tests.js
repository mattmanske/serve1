//-----------  Imports  -----------//

import React       from 'react'
import { shallow } from 'enzyme'

import ContactDetailsRoute from '../index'

//-----------  Tests  -----------//

describe('<ContactDetailsRoute />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
