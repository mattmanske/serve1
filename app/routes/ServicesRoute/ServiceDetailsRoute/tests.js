//-----------  Imports  -----------//

import React       from 'react'
import { shallow } from 'enzyme'

import ServiceDetailsRoute from '../index'

//-----------  Tests  -----------//

describe('<ServiceDetailsRoute />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
