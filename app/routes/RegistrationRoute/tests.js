//-----------  Imports  -----------//

import React       from 'react'
import { shallow } from 'enzyme'

import RegistrationRoute from '../index'

//-----------  Tests  -----------//

describe('<RegistrationRoute />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
