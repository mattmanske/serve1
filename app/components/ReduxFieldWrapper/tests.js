//-----------  Imports  -----------//

import React       from 'react'
import { shallow } from 'enzyme'

import ReduxFieldWrapper from '../index'

//-----------  Tests  -----------//

describe('<ReduxFieldWrapper />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
