//-----------  Imports  -----------//

import React       from 'react'
import { shallow } from 'enzyme'

import ReduxAntdSubmit from '../index'

//-----------  Tests  -----------//

describe('<ReduxAntdSubmit />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
