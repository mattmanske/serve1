//-----------  Imports  -----------//

import React       from 'react'
import { shallow } from 'enzyme'

import HorizontalWrapper from '../index'

//-----------  Tests  -----------//

describe('<HorizontalWrapper />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
